// Package port contains application interfaces for external dependencies
package port

// PaymentPersister defines the interface for payment persistence operations
type PaymentPersister interface {
	// TODO: Define payment persistence interface
}
